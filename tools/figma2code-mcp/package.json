{
  "name": "figma2code-mcp",
  "version": "0.1.0",
  "description": "CLI to fetch Figma MCP design context, normalize to plugin AST, and optionally POST to preview/generate endpoints",
  "type": "module",
  "bin": {
    "figma2code-mcp": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "ingest": "node dist/cli.js ingest",
    "test": "npm run build && node --test __tests__/"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
